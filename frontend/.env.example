# Deixe vazio no desenvolvimento para usar o proxy do Vite (/api).
# Em produção, informe a URL pública do backend.
# Vazio: proxy /api no desenvolvimento e mesma origem no build de producao.
# Preencha apenas se frontend e backend forem publicados separadamente.
VITE_API_URL=
